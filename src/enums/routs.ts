export const enum ROUTS_ASSETS {
  IMAGES = '/assets',
  MANIFEST = '/manifest.json',
  SW = '/sw.js'
}

export const enum ROUTS_AUTH {
  LOGIN = '/login'
}

export const enum ROUTS_HOME {
  BASE = '/',
  HOME = '/home'
}

export const enum ROUTS_DASHBOARD {
  BASE = '/dashboard'
}
